
- name: Fail-over
  panos_op:
    provider: "{{ provider }}"
    cmd: <request><high-availability><state><suspend></suspend></state></high-availability></request>
    cmd_is_xml: yes
  register: failover

- debug:
    var: failover