---
- name: Restart Process
  hosts: localhost
  gather_facts: no
  vars_prompt:
  collections:
  - paloaltonetworks.panos
  roles:
    - PaloAltoNetworks.paloaltonetworks
    - api_key
    - high_availability_state
    - fail_over